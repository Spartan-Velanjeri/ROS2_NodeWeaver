# find_package(launch_testing_ament_cmake REQUIRED)

# # Add launch tests
# file(GLOB TEST_FILES "launch_tests/*.launch.py")

# foreach(TEST_FILE ${TEST_FILES})
#   add_launch_test(
#     ${TEST_FILE}
#     TIMEOUT 120
#   )
# endforeach()
